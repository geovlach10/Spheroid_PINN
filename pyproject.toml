[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pinnpy"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["torch", "numpy", "scipy", "matplotlib"]

[tool.setuptools.packages.find]
include = ["pinnpy*", "trastuzumab*"]